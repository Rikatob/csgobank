CREATE TABLE IF NOT EXISTS item
(
    id          INT AUTO_INCREMENT PRIMARY KEY NOT NULL,
    type        VARCHAR(20),
    name        VARCHAR(20),
    float_value VARCHAR(20),
    price       INT

);