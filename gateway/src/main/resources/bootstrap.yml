spring:
  cloud:
    consul:
      config:
        format: yaml
        default-context: defaults
        data-key: application.yml
        prefixes: config
logging:
  level:
    org.springframework.amqp.rabbit.connection.CachigConnectionFactory: WARN